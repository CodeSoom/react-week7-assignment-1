import { render } from '@testing-library/react';

describe('ReviewList', () => {
  describe('when has reviews', () => {
    it('renders reviews', () => {
      render(<ReviewList />);
    });
  });

  // describe('when has no review', () => {

  // });
});
